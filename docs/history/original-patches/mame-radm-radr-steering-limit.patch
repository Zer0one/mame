diff --git a/src/mame/sega/segas32.cpp b/src/mame/sega/segas32.cpp
index 472ca534213..28cacf9768a 100644
--- a/src/mame/sega/segas32.cpp
+++ b/src/mame/sega/segas32.cpp
@@ -1978,7 +1978,7 @@ static INPUT_PORTS_START( radm )
 	PORT_BIT( 0x20, IP_ACTIVE_LOW, IPT_UNUSED )
 
 	PORT_START("mainpcb:ANALOG1")
-	PORT_BIT( 0xff, 0x80, IPT_PADDLE ) PORT_SENSITIVITY(30) PORT_KEYDELTA(10)
+	PORT_BIT( 0xff, 0x80, IPT_PADDLE ) PORT_SENSITIVITY(30) PORT_KEYDELTA(10) PORT_MINMAX(0x3b,0xc6)
 
 	PORT_START("mainpcb:ANALOG2")
 	PORT_BIT( 0xff, 0x00, IPT_PEDAL ) PORT_SENSITIVITY(30) PORT_KEYDELTA(10)
@@ -2007,7 +2007,7 @@ static INPUT_PORTS_START( radr )
 	PORT_DIPSETTING(    0x00, "Automatic" )
 
 	PORT_START("mainpcb:ANALOG1")
-	PORT_BIT( 0xff, 0x80, IPT_PADDLE ) PORT_SENSITIVITY(30) PORT_KEYDELTA(10)
+	PORT_BIT( 0xff, 0x80, IPT_PADDLE ) PORT_SENSITIVITY(30) PORT_KEYDELTA(10) PORT_MINMAX(0x3b,0xc6)
 
 	PORT_START("mainpcb:ANALOG2")
 	PORT_BIT( 0xff, 0x00, IPT_PEDAL ) PORT_SENSITIVITY(30) PORT_KEYDELTA(10)
